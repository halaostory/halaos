// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: announcements.sql

package store

import (
	"context"
	"time"

	"github.com/jackc/pgx/v5/pgtype"
)

const createAnnouncement = `-- name: CreateAnnouncement :one
INSERT INTO announcements (company_id, title, content, priority, target_roles, target_departments, published_at, expires_at, created_by)
VALUES ($1, $2, $3, $4, $5, $6, COALESCE($7, NOW()), $8, $9)
RETURNING id, company_id, title, content, priority, target_roles, target_departments, published_at, expires_at, created_by, created_at
`

type CreateAnnouncementParams struct {
	CompanyID         int64              `json:"company_id"`
	Title             string             `json:"title"`
	Content           string             `json:"content"`
	Priority          string             `json:"priority"`
	TargetRoles       []string           `json:"target_roles"`
	TargetDepartments []int64            `json:"target_departments"`
	Column7           interface{}        `json:"column_7"`
	ExpiresAt         pgtype.Timestamptz `json:"expires_at"`
	CreatedBy         *int64             `json:"created_by"`
}

func (q *Queries) CreateAnnouncement(ctx context.Context, arg CreateAnnouncementParams) (Announcement, error) {
	row := q.db.QueryRow(ctx, createAnnouncement,
		arg.CompanyID,
		arg.Title,
		arg.Content,
		arg.Priority,
		arg.TargetRoles,
		arg.TargetDepartments,
		arg.Column7,
		arg.ExpiresAt,
		arg.CreatedBy,
	)
	var i Announcement
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.Title,
		&i.Content,
		&i.Priority,
		&i.TargetRoles,
		&i.TargetDepartments,
		&i.PublishedAt,
		&i.ExpiresAt,
		&i.CreatedBy,
		&i.CreatedAt,
	)
	return i, err
}

const deleteAnnouncement = `-- name: DeleteAnnouncement :exec
DELETE FROM announcements WHERE id = $1 AND company_id = $2
`

type DeleteAnnouncementParams struct {
	ID        int64 `json:"id"`
	CompanyID int64 `json:"company_id"`
}

func (q *Queries) DeleteAnnouncement(ctx context.Context, arg DeleteAnnouncementParams) error {
	_, err := q.db.Exec(ctx, deleteAnnouncement, arg.ID, arg.CompanyID)
	return err
}

const listAllAnnouncements = `-- name: ListAllAnnouncements :many
SELECT a.id, a.title, a.content, a.priority,
       a.target_roles, a.target_departments,
       a.published_at, a.expires_at, a.created_at,
       COALESCE(u.first_name, '') as author_first_name,
       COALESCE(u.last_name, '') as author_last_name
FROM announcements a
LEFT JOIN users u ON u.id = a.created_by
WHERE a.company_id = $1
ORDER BY a.created_at DESC
LIMIT 100
`

type ListAllAnnouncementsRow struct {
	ID                int64              `json:"id"`
	Title             string             `json:"title"`
	Content           string             `json:"content"`
	Priority          string             `json:"priority"`
	TargetRoles       []string           `json:"target_roles"`
	TargetDepartments []int64            `json:"target_departments"`
	PublishedAt       pgtype.Timestamptz `json:"published_at"`
	ExpiresAt         pgtype.Timestamptz `json:"expires_at"`
	CreatedAt         time.Time          `json:"created_at"`
	AuthorFirstName   string             `json:"author_first_name"`
	AuthorLastName    string             `json:"author_last_name"`
}

func (q *Queries) ListAllAnnouncements(ctx context.Context, companyID int64) ([]ListAllAnnouncementsRow, error) {
	rows, err := q.db.Query(ctx, listAllAnnouncements, companyID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAllAnnouncementsRow{}
	for rows.Next() {
		var i ListAllAnnouncementsRow
		if err := rows.Scan(
			&i.ID,
			&i.Title,
			&i.Content,
			&i.Priority,
			&i.TargetRoles,
			&i.TargetDepartments,
			&i.PublishedAt,
			&i.ExpiresAt,
			&i.CreatedAt,
			&i.AuthorFirstName,
			&i.AuthorLastName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAnnouncements = `-- name: ListAnnouncements :many
SELECT a.id, a.title, a.content, a.priority,
       a.published_at, a.expires_at, a.created_at,
       COALESCE(u.first_name, '') as author_first_name,
       COALESCE(u.last_name, '') as author_last_name
FROM announcements a
LEFT JOIN users u ON u.id = a.created_by
WHERE a.company_id = $1
  AND (a.published_at IS NULL OR a.published_at <= NOW())
  AND (a.expires_at IS NULL OR a.expires_at > NOW())
ORDER BY a.priority DESC, a.published_at DESC NULLS LAST
LIMIT 50
`

type ListAnnouncementsRow struct {
	ID              int64              `json:"id"`
	Title           string             `json:"title"`
	Content         string             `json:"content"`
	Priority        string             `json:"priority"`
	PublishedAt     pgtype.Timestamptz `json:"published_at"`
	ExpiresAt       pgtype.Timestamptz `json:"expires_at"`
	CreatedAt       time.Time          `json:"created_at"`
	AuthorFirstName string             `json:"author_first_name"`
	AuthorLastName  string             `json:"author_last_name"`
}

func (q *Queries) ListAnnouncements(ctx context.Context, companyID int64) ([]ListAnnouncementsRow, error) {
	rows, err := q.db.Query(ctx, listAnnouncements, companyID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAnnouncementsRow{}
	for rows.Next() {
		var i ListAnnouncementsRow
		if err := rows.Scan(
			&i.ID,
			&i.Title,
			&i.Content,
			&i.Priority,
			&i.PublishedAt,
			&i.ExpiresAt,
			&i.CreatedAt,
			&i.AuthorFirstName,
			&i.AuthorLastName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
