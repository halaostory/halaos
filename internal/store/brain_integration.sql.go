// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: brain_integration.sql

package store

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
)

const createBrainLink = `-- name: CreateBrainLink :one
INSERT INTO brain_links (
    company_id, brain_tenant_id, api_endpoint, api_key_enc, webhook_secret
) VALUES ($1, $2, $3, $4, $5)
RETURNING id, company_id, brain_tenant_id, api_endpoint, api_key_enc, webhook_secret, is_active, last_synced_at, created_at, updated_at
`

type CreateBrainLinkParams struct {
	CompanyID     int64     `json:"company_id"`
	BrainTenantID uuid.UUID `json:"brain_tenant_id"`
	ApiEndpoint   string    `json:"api_endpoint"`
	ApiKeyEnc     string    `json:"api_key_enc"`
	WebhookSecret string    `json:"webhook_secret"`
}

func (q *Queries) CreateBrainLink(ctx context.Context, arg CreateBrainLinkParams) (BrainLink, error) {
	row := q.db.QueryRow(ctx, createBrainLink,
		arg.CompanyID,
		arg.BrainTenantID,
		arg.ApiEndpoint,
		arg.ApiKeyEnc,
		arg.WebhookSecret,
	)
	var i BrainLink
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.BrainTenantID,
		&i.ApiEndpoint,
		&i.ApiKeyEnc,
		&i.WebhookSecret,
		&i.IsActive,
		&i.LastSyncedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteBrainLink = `-- name: DeleteBrainLink :exec
DELETE FROM brain_links WHERE id = $1 AND company_id = $2
`

type DeleteBrainLinkParams struct {
	ID        int64 `json:"id"`
	CompanyID int64 `json:"company_id"`
}

func (q *Queries) DeleteBrainLink(ctx context.Context, arg DeleteBrainLinkParams) error {
	_, err := q.db.Exec(ctx, deleteBrainLink, arg.ID, arg.CompanyID)
	return err
}

const getActiveBrainLink = `-- name: GetActiveBrainLink :one
SELECT id, company_id, brain_tenant_id, api_endpoint, api_key_enc, webhook_secret, is_active, last_synced_at, created_at, updated_at FROM brain_links
WHERE company_id = $1 AND is_active = true
LIMIT 1
`

func (q *Queries) GetActiveBrainLink(ctx context.Context, companyID int64) (BrainLink, error) {
	row := q.db.QueryRow(ctx, getActiveBrainLink, companyID)
	var i BrainLink
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.BrainTenantID,
		&i.ApiEndpoint,
		&i.ApiKeyEnc,
		&i.WebhookSecret,
		&i.IsActive,
		&i.LastSyncedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getBrainLinkByID = `-- name: GetBrainLinkByID :one
SELECT id, company_id, brain_tenant_id, api_endpoint, api_key_enc, webhook_secret, is_active, last_synced_at, created_at, updated_at FROM brain_links
WHERE id = $1 AND company_id = $2
`

type GetBrainLinkByIDParams struct {
	ID        int64 `json:"id"`
	CompanyID int64 `json:"company_id"`
}

func (q *Queries) GetBrainLinkByID(ctx context.Context, arg GetBrainLinkByIDParams) (BrainLink, error) {
	row := q.db.QueryRow(ctx, getBrainLinkByID, arg.ID, arg.CompanyID)
	var i BrainLink
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.BrainTenantID,
		&i.ApiEndpoint,
		&i.ApiKeyEnc,
		&i.WebhookSecret,
		&i.IsActive,
		&i.LastSyncedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertBrainOutbox = `-- name: InsertBrainOutbox :one
INSERT INTO brain_outbox (
    company_id, event_type, aggregate_type, aggregate_id,
    payload, idempotency_key
) VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, company_id, event_type, aggregate_type, aggregate_id, payload, idempotency_key, status, retry_count, max_retries, next_retry_at, sent_at, error_message, created_at
`

type InsertBrainOutboxParams struct {
	CompanyID      int64           `json:"company_id"`
	EventType      string          `json:"event_type"`
	AggregateType  string          `json:"aggregate_type"`
	AggregateID    int64           `json:"aggregate_id"`
	Payload        json.RawMessage `json:"payload"`
	IdempotencyKey string          `json:"idempotency_key"`
}

func (q *Queries) InsertBrainOutbox(ctx context.Context, arg InsertBrainOutboxParams) (BrainOutbox, error) {
	row := q.db.QueryRow(ctx, insertBrainOutbox,
		arg.CompanyID,
		arg.EventType,
		arg.AggregateType,
		arg.AggregateID,
		arg.Payload,
		arg.IdempotencyKey,
	)
	var i BrainOutbox
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.EventType,
		&i.AggregateType,
		&i.AggregateID,
		&i.Payload,
		&i.IdempotencyKey,
		&i.Status,
		&i.RetryCount,
		&i.MaxRetries,
		&i.NextRetryAt,
		&i.SentAt,
		&i.ErrorMessage,
		&i.CreatedAt,
	)
	return i, err
}

const listBrainLinks = `-- name: ListBrainLinks :many
SELECT id, company_id, brain_tenant_id, api_endpoint, api_key_enc, webhook_secret, is_active, last_synced_at, created_at, updated_at FROM brain_links
WHERE company_id = $1
ORDER BY created_at DESC
`

func (q *Queries) ListBrainLinks(ctx context.Context, companyID int64) ([]BrainLink, error) {
	rows, err := q.db.Query(ctx, listBrainLinks, companyID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []BrainLink{}
	for rows.Next() {
		var i BrainLink
		if err := rows.Scan(
			&i.ID,
			&i.CompanyID,
			&i.BrainTenantID,
			&i.ApiEndpoint,
			&i.ApiKeyEnc,
			&i.WebhookSecret,
			&i.IsActive,
			&i.LastSyncedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPendingBrainOutbox = `-- name: ListPendingBrainOutbox :many
SELECT id, company_id, event_type, aggregate_type, aggregate_id, payload, idempotency_key, status, retry_count, max_retries, next_retry_at, sent_at, error_message, created_at FROM brain_outbox
WHERE status IN ('pending', 'failed')
  AND (next_retry_at IS NULL OR next_retry_at <= NOW())
ORDER BY created_at ASC
LIMIT $1
FOR UPDATE SKIP LOCKED
`

func (q *Queries) ListPendingBrainOutbox(ctx context.Context, limit int32) ([]BrainOutbox, error) {
	rows, err := q.db.Query(ctx, listPendingBrainOutbox, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []BrainOutbox{}
	for rows.Next() {
		var i BrainOutbox
		if err := rows.Scan(
			&i.ID,
			&i.CompanyID,
			&i.EventType,
			&i.AggregateType,
			&i.AggregateID,
			&i.Payload,
			&i.IdempotencyKey,
			&i.Status,
			&i.RetryCount,
			&i.MaxRetries,
			&i.NextRetryAt,
			&i.SentAt,
			&i.ErrorMessage,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markBrainOutboxFailed = `-- name: MarkBrainOutboxFailed :exec
UPDATE brain_outbox
SET status = CASE WHEN retry_count + 1 >= max_retries THEN 'dead' ELSE 'failed' END,
    retry_count = retry_count + 1,
    next_retry_at = NOW() + (INTERVAL '1 minute' * POWER(2, retry_count)),
    error_message = $2
WHERE id = $1
`

type MarkBrainOutboxFailedParams struct {
	ID           int64   `json:"id"`
	ErrorMessage *string `json:"error_message"`
}

func (q *Queries) MarkBrainOutboxFailed(ctx context.Context, arg MarkBrainOutboxFailedParams) error {
	_, err := q.db.Exec(ctx, markBrainOutboxFailed, arg.ID, arg.ErrorMessage)
	return err
}

const markBrainOutboxSent = `-- name: MarkBrainOutboxSent :exec
UPDATE brain_outbox
SET status = 'sent', sent_at = NOW(), error_message = NULL
WHERE id = $1
`

func (q *Queries) MarkBrainOutboxSent(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, markBrainOutboxSent, id)
	return err
}

const updateBrainLinkStatus = `-- name: UpdateBrainLinkStatus :exec
UPDATE brain_links
SET is_active = $2, updated_at = NOW()
WHERE id = $1
`

type UpdateBrainLinkStatusParams struct {
	ID       int64 `json:"id"`
	IsActive bool  `json:"is_active"`
}

func (q *Queries) UpdateBrainLinkStatus(ctx context.Context, arg UpdateBrainLinkStatusParams) error {
	_, err := q.db.Exec(ctx, updateBrainLinkStatus, arg.ID, arg.IsActive)
	return err
}

const updateBrainLinkSyncedAt = `-- name: UpdateBrainLinkSyncedAt :exec
UPDATE brain_links
SET last_synced_at = NOW(), updated_at = NOW()
WHERE id = $1
`

func (q *Queries) UpdateBrainLinkSyncedAt(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, updateBrainLinkSyncedAt, id)
	return err
}
