# AIGoNHR E2E Testing Suite — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a comprehensive Playwright-based testing suite covering all 46 backend API modules and 50+ frontend pages, running against production with test company isolation.

**Architecture:** Single Playwright project with three layers: data factory (creates isolated test company + seed data via API), API integration tests (Playwright `request` API), and browser E2E tests (Chromium). Data factory runs as `globalSetup`, creates a timestamped test company via `cli-register`, seeds data through API calls with rate-limit throttling.

**Tech Stack:** Playwright, TypeScript, targeting `https://halaos.com`

**Spec:** `docs/superpowers/specs/2026-03-27-e2e-testing-design.md`

---

## Task Dependencies

```
Task 1 (scaffold) → Task 2 (api-client) → Task 3 (auth fixture)
Task 3 → Task 4 (factory: company+deps) → Task 5 (factory: leaf modules)
Task 5 → Tasks 6-12 (API tests, parallel OK)
Task 5 → Tasks 13-19 (browser tests, parallel OK)
Task 5 → Task 20 (H5 tests)
All → Task 21 (CI/CD)
```

---

### Task 1: Project Scaffold

**Files:**
- Create: `e2e/package.json`
- Create: `e2e/tsconfig.json`
- Create: `e2e/playwright.config.ts`
- Create: `e2e/.gitignore`

- [ ] **Step 1: Create package.json**

```json
{
  "name": "aigonhr-e2e",
  "private": true,
  "scripts": {
    "test:api": "npx playwright test --project=api",
    "test:browser": "npx playwright test --project=browser",
    "test:h5": "npx playwright test --project=h5",
    "test:all": "npx playwright test",
    "test:cleanup": "npx tsx fixtures/cleanup.ts",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "tsx": "^4.0.0"
  }
}
```

- [ ] **Step 2: Create tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "outDir": "dist",
    "rootDir": ".",
    "baseUrl": ".",
    "paths": { "@/*": ["./*"] }
  },
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
```

- [ ] **Step 3: Create playwright.config.ts**

```typescript
import { defineConfig } from '@playwright/test';

const BASE_URL = process.env.E2E_BASE_URL || 'https://halaos.com';

export default defineConfig({
  globalSetup: './fixtures/data-factory.ts',
  retries: 1,
  expect: { timeout: 10_000 },
  reporter: [['html'], ['junit', { outputFile: 'test-results/junit.xml' }]],
  projects: [
    {
      name: 'api',
      testDir: './tests/api',
      use: { baseURL: BASE_URL },
      timeout: 30_000,
    },
    {
      name: 'browser',
      testDir: './tests/browser',
      use: {
        baseURL: BASE_URL,
        browserName: 'chromium',
        screenshot: 'only-on-failure',
        trace: 'on-first-retry',
      },
      timeout: 60_000,
    },
    {
      name: 'h5',
      testDir: './tests/h5',
      use: {
        baseURL: BASE_URL + '/m',
        browserName: 'chromium',
        viewport: { width: 375, height: 812 },
        screenshot: 'only-on-failure',
        trace: 'on-first-retry',
      },
      timeout: 60_000,
    },
  ],
});
```

- [ ] **Step 4: Create .gitignore**

```
node_modules/
.test-state.json
test-results/
playwright-report/
dist/
```

- [ ] **Step 5: Install dependencies and verify**

Run: `cd e2e && npm install && npx playwright install chromium`
Expected: Dependencies installed, Chromium downloaded

- [ ] **Step 6: Commit**

```bash
git add e2e/package.json e2e/tsconfig.json e2e/playwright.config.ts e2e/.gitignore e2e/package-lock.json
git commit -m "feat(e2e): project scaffold with Playwright config"
```

---

### Task 2: API Client with Throttling

**Files:**
- Create: `e2e/fixtures/api-client.ts`
- Create: `e2e/fixtures/state.ts`

- [ ] **Step 1: Create state manager (state.ts)**

```typescript
import { readFileSync, writeFileSync, existsSync } from 'fs';
import { resolve } from 'path';

const STATE_PATH = resolve(__dirname, '../.test-state.json');

export interface TestState {
  companyName: string;
  adminToken: string;
  adminEmail: string;
  adminPassword: string;
  employeeTokens: Record<string, string>;
  departmentIds: number[];
  positionIds: number[];
  costCenterIds: number[];
  employeeIds: number[];
  employeeNos: string[];
  leaveTypeIds: number[];
  payrollCycleId: number | null;
  benefitPlanIds: number[];
  trainingProgramIds: number[];
  shiftIds: number[];
  [key: string]: unknown;
}

const DEFAULT_STATE: TestState = {
  companyName: '',
  adminToken: '',
  adminEmail: '',
  adminPassword: '',
  employeeTokens: {},
  departmentIds: [],
  positionIds: [],
  costCenterIds: [],
  employeeIds: [],
  employeeNos: [],
  leaveTypeIds: [],
  payrollCycleId: null,
  benefitPlanIds: [],
  trainingProgramIds: [],
  shiftIds: [],
};

let cached: TestState | null = null;

export function loadState(): TestState {
  if (cached) return cached;
  if (!existsSync(STATE_PATH)) return { ...DEFAULT_STATE };
  cached = JSON.parse(readFileSync(STATE_PATH, 'utf-8'));
  return cached!;
}

export function saveState(state: TestState): void {
  cached = state;
  writeFileSync(STATE_PATH, JSON.stringify(state, null, 2));
}

export function updateState(patch: Partial<TestState>): void {
  const state = loadState();
  Object.assign(state, patch);
  saveState(state);
}
```

- [ ] **Step 2: Create API client (api-client.ts)**

```typescript
import { request, APIRequestContext } from '@playwright/test';

const THROTTLE_MS = 700;
const MAX_RETRIES = 3;

let lastRequestTime = 0;

async function throttle(): Promise<void> {
  const now = Date.now();
  const elapsed = now - lastRequestTime;
  if (elapsed < THROTTLE_MS) {
    await new Promise(r => setTimeout(r, THROTTLE_MS - elapsed));
  }
  lastRequestTime = Date.now();
}

export class ApiClient {
  private ctx: APIRequestContext;
  private token: string;
  private baseURL: string;

  constructor(ctx: APIRequestContext, baseURL: string, token: string = '') {
    this.ctx = ctx;
    this.baseURL = baseURL;
    this.token = token;
  }

  setToken(token: string) {
    this.token = token;
  }

  private headers(): Record<string, string> {
    const h: Record<string, string> = { 'Content-Type': 'application/json' };
    if (this.token) h['Authorization'] = `Bearer ${this.token}`;
    return h;
  }

  async get(path: string, params?: Record<string, string>): Promise<any> {
    await throttle();
    const url = new URL(this.baseURL + path);
    if (params) Object.entries(params).forEach(([k, v]) => url.searchParams.set(k, v));
    const res = await this.doWithRetry(() =>
      this.ctx.get(url.toString(), { headers: this.headers() })
    );
    return this.unwrap(res);
  }

  async post(path: string, data?: any): Promise<any> {
    await throttle();
    const res = await this.doWithRetry(() =>
      this.ctx.post(this.baseURL + path, { headers: this.headers(), data })
    );
    return this.unwrap(res);
  }

  async put(path: string, data?: any): Promise<any> {
    await throttle();
    const res = await this.doWithRetry(() =>
      this.ctx.put(this.baseURL + path, { headers: this.headers(), data })
    );
    return this.unwrap(res);
  }

  async delete(path: string): Promise<any> {
    await throttle();
    const res = await this.doWithRetry(() =>
      this.ctx.delete(this.baseURL + path, { headers: this.headers() })
    );
    return this.unwrap(res);
  }

  async postForm(path: string, formData: any): Promise<any> {
    await throttle();
    const res = await this.doWithRetry(() =>
      this.ctx.post(this.baseURL + path, {
        headers: { Authorization: `Bearer ${this.token}` },
        multipart: formData,
      })
    );
    return this.unwrap(res);
  }

  async getRaw(path: string, params?: Record<string, string>) {
    await throttle();
    const url = new URL(this.baseURL + path);
    if (params) Object.entries(params).forEach(([k, v]) => url.searchParams.set(k, v));
    return this.doWithRetry(() =>
      this.ctx.get(url.toString(), { headers: this.headers() })
    );
  }

  private async doWithRetry(fn: () => Promise<any>, retries = MAX_RETRIES): Promise<any> {
    for (let i = 0; i < retries; i++) {
      const res = await fn();
      if (res.status() === 429) {
        const wait = parseInt(res.headers()['retry-after'] || '2') * 1000 + 1000;
        console.warn(`Rate limited, waiting ${wait}ms...`);
        await new Promise(r => setTimeout(r, wait));
        continue;
      }
      return res;
    }
    throw new Error('Max retries exceeded due to rate limiting');
  }

  private async unwrap(res: any): Promise<any> {
    const body = await res.json();
    if (body.success === false) {
      throw new Error(`API error (${res.status()}): ${body.error?.message || JSON.stringify(body.error)}`);
    }
    return body.data ?? body;
  }
}

export async function createApiClient(baseURL: string, token: string = ''): Promise<ApiClient> {
  const ctx = await request.newContext();
  return new ApiClient(ctx, baseURL, token);
}
```

- [ ] **Step 3: Verify TypeScript compiles**

Run: `cd e2e && npx tsc --noEmit`
Expected: No errors

- [ ] **Step 4: Commit**

```bash
git add e2e/fixtures/api-client.ts e2e/fixtures/state.ts
git commit -m "feat(e2e): API client with throttling and state manager"
```

---

### Task 3: Auth Fixture

**Files:**
- Create: `e2e/fixtures/auth.ts`

- [ ] **Step 1: Create auth fixture**

```typescript
import { test as base, type BrowserContext } from '@playwright/test';
import { loadState } from './state';

type AuthFixtures = {
  adminToken: string;
  employeeToken: string;
  adminContext: BrowserContext;
  employeeContext: BrowserContext;
};

export const test = base.extend<AuthFixtures>({
  adminToken: async ({}, use) => {
    const state = loadState();
    if (!state.adminToken) throw new Error('No admin token — run data factory first');
    await use(state.adminToken);
  },

  employeeToken: async ({}, use) => {
    const state = loadState();
    const tokens = Object.values(state.employeeTokens);
    if (tokens.length === 0) throw new Error('No employee tokens — run data factory first');
    await use(tokens[0]);
  },

  adminContext: async ({ browser }, use) => {
    const state = loadState();
    const context = await browser.newContext();
    await context.addInitScript((token: string) => {
      localStorage.setItem('access_token', token);
      localStorage.setItem('halaos_setup_done', 'true');
    }, state.adminToken);
    await use(context);
    await context.close();
  },

  employeeContext: async ({ browser }, use) => {
    const state = loadState();
    const tokens = Object.values(state.employeeTokens);
    if (tokens.length === 0) throw new Error('No employee tokens');
    const context = await browser.newContext();
    await context.addInitScript((token: string) => {
      localStorage.setItem('access_token', token);
      localStorage.setItem('halaos_setup_done', 'true');
    }, tokens[0]);
    await use(context);
    await context.close();
  },
});

export { expect } from '@playwright/test';
```

- [ ] **Step 2: Verify compiles**

Run: `cd e2e && npx tsc --noEmit`

- [ ] **Step 3: Commit**

```bash
git add e2e/fixtures/auth.ts
git commit -m "feat(e2e): auth fixtures for admin and employee contexts"
```

---

### Task 4: Data Factory — Dependency Modules

**Files:**
- Create: `e2e/fixtures/data-factory.ts`
- Create: `e2e/factory/company.ts`
- Create: `e2e/factory/departments.ts`
- Create: `e2e/factory/employees.ts`

These are **dependency modules** — if any fails, the entire test run aborts.

- [ ] **Step 1: Create company factory**

`e2e/factory/company.ts` — Registers a test company via `cli-register`, logs in via `cli-login`.

```typescript
import { ApiClient } from '../fixtures/api-client';
import { updateState } from '../fixtures/state';

export async function seedCompany(api: ApiClient): Promise<void> {
  const ts = Date.now();
  const email = `admin-e2e-${ts}@test.halaos.com`;
  const password = `E2eTest${ts}!`;
  const companyName = `E2E-Test-${new Date().toISOString().replace(/[:.]/g, '-').slice(0, 19)}`;

  // Register (cli-register bypasses email verification)
  await api.post('/api/v1/auth/cli-register', {
    email,
    password,
    first_name: 'E2E',
    last_name: 'Admin',
    company_name: companyName,
  });

  // Login
  const loginRes = await api.post('/api/v1/auth/cli-login', { email, password });
  const token = loginRes.token || loginRes.access_token;
  if (!token) throw new Error('cli-login did not return token');

  api.setToken(token);
  updateState({ companyName, adminToken: token, adminEmail: email, adminPassword: password });
  console.log(`  Company created: ${companyName}`);
}
```

- [ ] **Step 2: Create departments factory**

`e2e/factory/departments.ts` — Creates 8 departments, 15 positions, 3 cost centers.

```typescript
import { ApiClient } from '../fixtures/api-client';
import { updateState } from '../fixtures/state';

const DEPARTMENTS = [
  'Human Resources', 'Engineering', 'Finance', 'Operations',
  'Sales', 'Marketing', 'Legal', 'Customer Support',
];

const POSITIONS = [
  'Manager', 'Senior Developer', 'Junior Developer', 'Designer',
  'Accountant', 'HR Specialist', 'Sales Rep', 'Support Agent',
  'Team Lead', 'Director', 'Analyst', 'Coordinator',
  'Admin Assistant', 'QA Engineer', 'DevOps Engineer',
];

const COST_CENTERS = ['CC-HQ', 'CC-BRANCH-1', 'CC-REMOTE'];

export async function seedDepartments(api: ApiClient): Promise<void> {
  const departmentIds: number[] = [];
  for (const name of DEPARTMENTS) {
    const res = await api.post('/api/v1/departments', { name, description: `${name} department` });
    departmentIds.push(res.id);
  }

  const positionIds: number[] = [];
  for (const title of POSITIONS) {
    const deptId = departmentIds[Math.floor(Math.random() * departmentIds.length)];
    const res = await api.post('/api/v1/positions', {
      title,
      department_id: deptId,
      description: `${title} role`,
    });
    positionIds.push(res.id);
  }

  const costCenterIds: number[] = [];
  for (const code of COST_CENTERS) {
    const res = await api.post('/api/v1/cost-centers', { code, name: code, description: code });
    costCenterIds.push(res.id);
  }

  updateState({ departmentIds, positionIds, costCenterIds });
  console.log(`  Departments: ${departmentIds.length}, Positions: ${positionIds.length}, Cost Centers: ${costCenterIds.length}`);
}
```

- [ ] **Step 3: Create employees factory**

`e2e/factory/employees.ts` — Creates 60 employees + user accounts for 10 of them.

```typescript
import { ApiClient, createApiClient } from '../fixtures/api-client';
import { loadState, updateState } from '../fixtures/state';

const FIRST_NAMES = ['Juan', 'Maria', 'Jose', 'Ana', 'Pedro', 'Rosa', 'Carlos', 'Elena', 'Miguel', 'Carmen',
  'Antonio', 'Lucia', 'Rafael', 'Isabel', 'Luis', 'Teresa', 'Manuel', 'Gloria', 'Francisco', 'Patricia'];
const LAST_NAMES = ['Santos', 'Reyes', 'Cruz', 'Garcia', 'Torres', 'Lopez', 'Ramos', 'Flores', 'Rivera', 'Gomez',
  'Diaz', 'Mendoza', 'Morales', 'Castro', 'Ortiz', 'Vargas', 'Romero', 'Herrera', 'Medina', 'Aguilar'];
const EMPLOYMENT_TYPES = ['regular', 'contractual', 'probationary'];
const STATUSES = ['active', 'active', 'active', 'active', 'separated']; // 80% active

export async function seedEmployees(api: ApiClient): Promise<void> {
  const state = loadState();
  const employeeIds: number[] = [];
  const employeeNos: string[] = [];

  for (let i = 0; i < 60; i++) {
    const empNo = `E2E-${String(i + 1).padStart(3, '0')}`;
    const firstName = FIRST_NAMES[i % FIRST_NAMES.length];
    const lastName = LAST_NAMES[i % LAST_NAMES.length];
    const deptId = state.departmentIds[i % state.departmentIds.length];
    const posId = state.positionIds[i % state.positionIds.length];
    const hireDate = new Date(2024, 0, 1 + i).toISOString().split('T')[0];

    const res = await api.post('/api/v1/employees', {
      employee_no: empNo,
      first_name: firstName,
      last_name: lastName,
      email: `${empNo.toLowerCase()}@test.halaos.com`,
      hire_date: hireDate,
      employment_type: EMPLOYMENT_TYPES[i % EMPLOYMENT_TYPES.length],
      status: STATUSES[i % STATUSES.length],
      department_id: deptId,
      position_id: posId,
      gender: i % 2 === 0 ? 'male' : 'female',
      birth_date: new Date(1990 + (i % 15), i % 12, 1 + (i % 28)).toISOString().split('T')[0],
    });
    employeeIds.push(res.id);
    employeeNos.push(empNo);
  }

  // Create user accounts for first 10 employees and get tokens
  const employeeTokens: Record<string, string> = {};
  const baseURL = process.env.E2E_BASE_URL || 'https://halaos.com';

  for (let i = 0; i < 10; i++) {
    const empNo = employeeNos[i];
    const email = `${empNo.toLowerCase()}@test.halaos.com`;
    const password = `EmpPass${i + 1}!`;

    try {
      // Create user account linked to employee
      await api.post('/api/v1/users', {
        email,
        password,
        role: i === 0 ? 'manager' : 'employee',
        employee_id: employeeIds[i],
      });

      // Login to get token
      const loginApi = await createApiClient(baseURL);
      const loginRes = await loginApi.post('/api/v1/auth/cli-login', { email, password });
      employeeTokens[empNo] = loginRes.token || loginRes.access_token;
    } catch (err) {
      console.warn(`  Warning: Could not create user for ${empNo}: ${err}`);
    }
  }

  updateState({ employeeIds, employeeNos, employeeTokens });
  console.log(`  Employees: ${employeeIds.length}, User accounts: ${Object.keys(employeeTokens).length}`);
}
```

- [ ] **Step 4: Create data factory orchestrator**

`e2e/fixtures/data-factory.ts` — globalSetup entry point.

```typescript
import { createApiClient } from './api-client';
import { saveState, loadState } from './state';
import { seedCompany } from '../factory/company';
import { seedDepartments } from '../factory/departments';
import { seedEmployees } from '../factory/employees';

export default async function globalSetup() {
  const baseURL = process.env.E2E_BASE_URL || 'https://halaos.com';
  console.log(`\nE2E Data Factory — target: ${baseURL}`);

  const api = await createApiClient(baseURL);

  // Dependency modules — abort on failure
  console.log('[1/3] Creating test company...');
  await seedCompany(api);

  console.log('[2/3] Creating departments, positions, cost centers...');
  await seedDepartments(api);

  console.log('[3/3] Creating employees + user accounts...');
  await seedEmployees(api);

  // Leaf modules — log error, continue
  const leafModules: Array<{ name: string; fn: () => Promise<void> }> = [];

  try {
    const { seedAttendance } = await import('../factory/attendance');
    leafModules.push({ name: 'Attendance', fn: () => seedAttendance(api) });
  } catch { /* module not yet created */ }

  try {
    const { seedLeave } = await import('../factory/leave');
    leafModules.push({ name: 'Leave', fn: () => seedLeave(api) });
  } catch { /* module not yet created */ }

  try {
    const { seedPayroll } = await import('../factory/payroll');
    leafModules.push({ name: 'Payroll', fn: () => seedPayroll(api) });
  } catch { /* module not yet created */ }

  try {
    const { seedBenefits } = await import('../factory/benefits');
    leafModules.push({ name: 'Benefits', fn: () => seedBenefits(api) });
  } catch { /* module not yet created */ }

  try {
    const { seedTraining } = await import('../factory/training');
    leafModules.push({ name: 'Training', fn: () => seedTraining(api) });
  } catch { /* module not yet created */ }

  try {
    const { seedPerformance } = await import('../factory/performance');
    leafModules.push({ name: 'Performance', fn: () => seedPerformance(api) });
  } catch { /* module not yet created */ }

  try {
    const { seedCompliance } = await import('../factory/compliance');
    leafModules.push({ name: 'Compliance', fn: () => seedCompliance(api) });
  } catch { /* module not yet created */ }

  try {
    const { seedMisc } = await import('../factory/misc');
    leafModules.push({ name: 'Misc', fn: () => seedMisc(api) });
  } catch { /* module not yet created */ }

  let leafIdx = 0;
  for (const mod of leafModules) {
    leafIdx++;
    try {
      console.log(`[leaf ${leafIdx}/${leafModules.length}] Seeding ${mod.name}...`);
      await mod.fn();
    } catch (err) {
      console.error(`  ERROR seeding ${mod.name}: ${err}`);
    }
  }

  console.log('\nData factory complete.\n');
}
```

- [ ] **Step 5: Verify compiles**

Run: `cd e2e && npx tsc --noEmit`

- [ ] **Step 6: Commit**

```bash
git add e2e/fixtures/data-factory.ts e2e/factory/company.ts e2e/factory/departments.ts e2e/factory/employees.ts
git commit -m "feat(e2e): data factory — company, departments, employees"
```

---

### Task 5: Data Factory — Leaf Modules

**Files:**
- Create: `e2e/factory/attendance.ts`
- Create: `e2e/factory/leave.ts`
- Create: `e2e/factory/payroll.ts`
- Create: `e2e/factory/benefits.ts`
- Create: `e2e/factory/training.ts`
- Create: `e2e/factory/performance.ts`
- Create: `e2e/factory/compliance.ts`
- Create: `e2e/factory/misc.ts`

Each module exports a `seed*` function that takes an `ApiClient` and creates domain-specific data.

- [ ] **Step 1: Create attendance factory**

Uses employee tokens to clock in/out. Distributes across 10 employee tokens to stay under rate limits.

```typescript
import { ApiClient, createApiClient } from '../fixtures/api-client';
import { loadState, updateState } from '../fixtures/state';

export async function seedAttendance(api: ApiClient): Promise<void> {
  const state = loadState();
  const baseURL = process.env.E2E_BASE_URL || 'https://halaos.com';
  const tokens = Object.values(state.employeeTokens);
  if (tokens.length === 0) { console.warn('  No employee tokens, skipping attendance'); return; }

  let count = 0;
  // Seed 7 days of attendance for each employee with a token
  for (const token of tokens) {
    const empApi = await createApiClient(baseURL, token);
    for (let day = 0; day < 7; day++) {
      try {
        await empApi.post('/api/v1/attendance/clock-in', { source: 'web' });
        await empApi.post('/api/v1/attendance/clock-out', { source: 'web' });
        count++;
      } catch { /* skip errors for past dates */ }
    }
  }

  console.log(`  Attendance records: ${count} days`);
}
```

- [ ] **Step 2: Create leave factory**

```typescript
import { ApiClient } from '../fixtures/api-client';
import { loadState, updateState } from '../fixtures/state';

export async function seedLeave(api: ApiClient): Promise<void> {
  const state = loadState();

  // Create leave types
  const leaveTypeIds: number[] = [];
  const types = [
    { name: 'Vacation Leave', days_per_year: 15, is_paid: true },
    { name: 'Sick Leave', days_per_year: 15, is_paid: true },
    { name: 'Emergency Leave', days_per_year: 5, is_paid: true },
  ];
  for (const lt of types) {
    try {
      const res = await api.post('/api/v1/leave-types', lt);
      leaveTypeIds.push(res.id);
    } catch (err) { console.warn(`  Leave type ${lt.name}: ${err}`); }
  }

  // Submit leave requests using employee tokens
  const baseURL = process.env.E2E_BASE_URL || 'https://halaos.com';
  const tokens = Object.entries(state.employeeTokens).slice(0, 5);
  for (const [empNo, token] of tokens) {
    try {
      const { createApiClient } = await import('../fixtures/api-client');
      const empApi = await createApiClient(baseURL, token);
      await empApi.post('/api/v1/leaves', {
        leave_type_id: leaveTypeIds[0],
        start_date: '2026-04-01',
        end_date: '2026-04-02',
        reason: `E2E test leave for ${empNo}`,
      });
    } catch { /* skip */ }
  }

  updateState({ leaveTypeIds });
  console.log(`  Leave types: ${leaveTypeIds.length}, Requests submitted`);
}
```

- [ ] **Step 3: Create payroll factory**

```typescript
import { ApiClient } from '../fixtures/api-client';
import { loadState, updateState } from '../fixtures/state';

export async function seedPayroll(api: ApiClient): Promise<void> {
  const state = loadState();

  // Assign basic salary to first 20 employees
  for (let i = 0; i < Math.min(20, state.employeeIds.length); i++) {
    try {
      await api.post(`/api/v1/employees/${state.employeeIds[i]}/salary`, {
        basic_salary: 25000 + (i * 1000),
        effective_date: '2024-01-01',
      });
    } catch { /* skip */ }
  }

  // Create payroll cycle
  let payrollCycleId: number | null = null;
  try {
    const res = await api.post('/api/v1/payroll-cycles', {
      name: 'E2E Test Cycle',
      type: 'semi_monthly',
      start_date: '2026-03-01',
      end_date: '2026-03-15',
      pay_date: '2026-03-20',
    });
    payrollCycleId = res.id;
  } catch (err) { console.warn(`  Payroll cycle: ${err}`); }

  updateState({ payrollCycleId });
  console.log(`  Salaries assigned, Payroll cycle: ${payrollCycleId}`);
}
```

- [ ] **Step 4: Create remaining leaf factories (benefits, training, performance, compliance, misc)**

Each follows the same pattern — create domain data via API calls, update state.

`e2e/factory/benefits.ts`:
```typescript
import { ApiClient } from '../fixtures/api-client';
import { loadState, updateState } from '../fixtures/state';

export async function seedBenefits(api: ApiClient): Promise<void> {
  const state = loadState();
  const benefitPlanIds: number[] = [];

  const plans = [
    { name: 'Health Insurance', type: 'health', description: 'Company health plan' },
    { name: 'Life Insurance', type: 'life', description: 'Company life plan' },
  ];
  for (const plan of plans) {
    try {
      const res = await api.post('/api/v1/benefits', plan);
      benefitPlanIds.push(res.id);
    } catch { /* skip */ }
  }

  // Enroll first 5 employees
  for (let i = 0; i < Math.min(5, state.employeeIds.length); i++) {
    for (const planId of benefitPlanIds) {
      try {
        await api.post('/api/v1/benefits/enrollments', {
          employee_id: state.employeeIds[i],
          benefit_plan_id: planId,
        });
      } catch { /* skip */ }
    }
  }

  updateState({ benefitPlanIds });
  console.log(`  Benefit plans: ${benefitPlanIds.length}`);
}
```

`e2e/factory/training.ts`:
```typescript
import { ApiClient } from '../fixtures/api-client';
import { loadState, updateState } from '../fixtures/state';

export async function seedTraining(api: ApiClient): Promise<void> {
  const state = loadState();
  const trainingProgramIds: number[] = [];

  const programs = [
    { title: 'Safety Training', description: 'Workplace safety', duration_hours: 8 },
    { title: 'Leadership Workshop', description: 'Management skills', duration_hours: 16 },
    { title: 'Compliance 101', description: 'Regulatory compliance', duration_hours: 4 },
  ];
  for (const prog of programs) {
    try {
      const res = await api.post('/api/v1/training/programs', prog);
      trainingProgramIds.push(res.id);
    } catch { /* skip */ }
  }

  updateState({ trainingProgramIds });
  console.log(`  Training programs: ${trainingProgramIds.length}`);
}
```

`e2e/factory/performance.ts`:
```typescript
import { ApiClient } from '../fixtures/api-client';
import { loadState } from '../fixtures/state';

export async function seedPerformance(api: ApiClient): Promise<void> {
  const state = loadState();

  // Create performance review cycle
  try {
    await api.post('/api/v1/performance/reviews', {
      title: 'Q1 2026 Review',
      period_start: '2026-01-01',
      period_end: '2026-03-31',
      status: 'in_progress',
    });
  } catch { /* skip */ }

  // Create goals for first 5 employees
  for (let i = 0; i < Math.min(5, state.employeeIds.length); i++) {
    try {
      await api.post('/api/v1/performance/goals', {
        employee_id: state.employeeIds[i],
        title: `Q1 Goal for employee ${i + 1}`,
        description: 'E2E test goal',
        target_date: '2026-03-31',
      });
    } catch { /* skip */ }
  }

  console.log(`  Performance reviews and goals created`);
}
```

`e2e/factory/compliance.ts`:
```typescript
import { ApiClient } from '../fixtures/api-client';

export async function seedCompliance(api: ApiClient): Promise<void> {
  // Compliance forms are typically pre-seeded by migrations.
  // Just verify they exist.
  try {
    await api.get('/api/v1/compliance/forms');
    console.log(`  Compliance forms verified`);
  } catch (err) { console.warn(`  Compliance: ${err}`); }
}
```

`e2e/factory/misc.ts`:
```typescript
import { ApiClient } from '../fixtures/api-client';
import { loadState } from '../fixtures/state';

export async function seedMisc(api: ApiClient): Promise<void> {
  const state = loadState();

  // Announcements
  try {
    await api.post('/api/v1/announcements', {
      title: 'E2E Test Announcement',
      content: 'This is a test announcement from E2E suite.',
      is_published: true,
    });
  } catch { /* skip */ }

  // Knowledge base article
  try {
    await api.post('/api/v1/knowledge', {
      title: 'E2E Test Article',
      content: 'Knowledge base article for E2E testing.',
      category: 'General',
    });
  } catch { /* skip */ }

  // Holiday
  try {
    await api.post('/api/v1/holidays', {
      name: 'E2E Test Holiday',
      date: '2026-12-25',
      type: 'regular',
    });
  } catch { /* skip */ }

  // Approval workflow
  try {
    await api.post('/api/v1/approval/workflows', {
      name: 'E2E Leave Approval',
      type: 'leave',
      steps: [{ approver_role: 'manager', order: 1 }],
    });
  } catch { /* skip */ }

  console.log(`  Misc data seeded (announcements, knowledge, holidays, approvals)`);
}
```

- [ ] **Step 5: Verify compiles**

Run: `cd e2e && npx tsc --noEmit`

- [ ] **Step 6: Run globalSetup against production to verify factory works**

Run: `cd e2e && E2E_BASE_URL=https://halaos.com npx tsx fixtures/data-factory.ts`
Expected: All dependency modules succeed, leaf modules log results

- [ ] **Step 7: Commit**

```bash
git add e2e/factory/
git commit -m "feat(e2e): data factory leaf modules — attendance, leave, payroll, benefits, training, performance, compliance, misc"
```

---

### Task 6: API Tests — Auth & Employee

**Files:**
- Create: `e2e/tests/api/auth.spec.ts`
- Create: `e2e/tests/api/employees.spec.ts`

**Pattern:** Each test file imports `test` and `expect` from `@playwright/test`, loads state for tokens, creates an `ApiClient`, and tests CRUD + permissions + errors.

- [ ] **Step 1: Create auth API tests**

```typescript
import { test, expect } from '@playwright/test';
import { loadState } from '../../fixtures/state';
import { createApiClient } from '../../fixtures/api-client';

const BASE = process.env.E2E_BASE_URL || 'https://halaos.com';

test.describe('Auth API', () => {
  test('login with valid credentials returns token', async () => {
    const state = loadState();
    const api = await createApiClient(BASE);
    const res = await api.post('/api/v1/auth/cli-login', {
      email: state.adminEmail,
      password: state.adminPassword,
    });
    expect(res.token || res.access_token).toBeTruthy();
  });

  test('login with wrong password returns error', async () => {
    const state = loadState();
    const api = await createApiClient(BASE);
    await expect(api.post('/api/v1/auth/cli-login', {
      email: state.adminEmail,
      password: 'WrongPassword123!',
    })).rejects.toThrow();
  });

  test('GET /auth/me returns current user', async () => {
    const state = loadState();
    const api = await createApiClient(BASE, state.adminToken);
    const res = await api.get('/api/v1/auth/me');
    expect(res.email).toBe(state.adminEmail);
  });

  test('GET /auth/me without token returns 401', async () => {
    const api = await createApiClient(BASE);
    await expect(api.get('/api/v1/auth/me')).rejects.toThrow();
  });

  test('API key CRUD', async () => {
    const state = loadState();
    const api = await createApiClient(BASE, state.adminToken);

    // Create
    const key = await api.post('/api/v1/api-keys', { name: 'E2E Test Key' });
    expect(key.id).toBeTruthy();
    expect(key.key || key.api_key).toBeTruthy();

    // List
    const keys = await api.get('/api/v1/api-keys');
    expect(Array.isArray(keys)).toBe(true);

    // Delete
    await api.delete(`/api/v1/api-keys/${key.id}`);
  });
});
```

- [ ] **Step 2: Create employees API tests**

```typescript
import { test, expect } from '@playwright/test';
import { loadState } from '../../fixtures/state';
import { createApiClient } from '../../fixtures/api-client';

const BASE = process.env.E2E_BASE_URL || 'https://halaos.com';

test.describe('Employees API', () => {
  test('list employees returns data', async () => {
    const state = loadState();
    const api = await createApiClient(BASE, state.adminToken);
    const res = await api.get('/api/v1/employees');
    expect(Array.isArray(res)).toBe(true);
    expect(res.length).toBeGreaterThan(0);
  });

  test('list employees with department filter', async () => {
    const state = loadState();
    const api = await createApiClient(BASE, state.adminToken);
    const res = await api.get('/api/v1/employees', {
      department_id: String(state.departmentIds[0]),
    });
    expect(Array.isArray(res)).toBe(true);
  });

  test('list employees with status filter', async () => {
    const state = loadState();
    const api = await createApiClient(BASE, state.adminToken);
    const res = await api.get('/api/v1/employees', { status: 'active' });
    expect(Array.isArray(res)).toBe(true);
    expect(res.length).toBeGreaterThan(0);
  });

  test('list employees with pagination', async () => {
    const state = loadState();
    const api = await createApiClient(BASE, state.adminToken);
    const res = await api.get('/api/v1/employees', { limit: '5', page: '1' });
    expect(Array.isArray(res)).toBe(true);
    expect(res.length).toBeLessThanOrEqual(5);
  });

  test('get employee by id', async () => {
    const state = loadState();
    const api = await createApiClient(BASE, state.adminToken);
    const res = await api.get(`/api/v1/employees/${state.employeeIds[0]}`);
    expect(res.id).toBe(state.employeeIds[0]);
    expect(res.first_name).toBeTruthy();
  });

  test('get nonexistent employee returns error', async () => {
    const state = loadState();
    const api = await createApiClient(BASE, state.adminToken);
    await expect(api.get('/api/v1/employees/999999')).rejects.toThrow();
  });

  test('create and update employee', async () => {
    const state = loadState();
    const api = await createApiClient(BASE, state.adminToken);

    const created = await api.post('/api/v1/employees', {
      employee_no: `E2E-NEW-${Date.now()}`,
      first_name: 'NewTest',
      last_name: 'Employee',
      hire_date: '2026-03-01',
      employment_type: 'regular',
      department_id: state.departmentIds[0],
      position_id: state.positionIds[0],
    });
    expect(created.id).toBeTruthy();

    const updated = await api.put(`/api/v1/employees/${created.id}`, {
      first_name: 'UpdatedTest',
    });
    expect(updated.first_name).toBe('UpdatedTest');
  });

  test('export employees CSV', async () => {
    const state = loadState();
    const api = await createApiClient(BASE, state.adminToken);
    const res = await api.getRaw('/api/v1/export/employees/csv');
    expect(res.status()).toBe(200);
    const text = await res.text();
    expect(text).toContain('Employee No');
  });

  test('employee role cannot create employee', async () => {
    const state = loadState();
    const tokens = Object.values(state.employeeTokens);
    if (tokens.length === 0) { test.skip(); return; }
    const api = await createApiClient(BASE, tokens[0]);
    await expect(api.post('/api/v1/employees', {
      employee_no: 'SHOULD-FAIL',
      first_name: 'Fail',
      last_name: 'Test',
      hire_date: '2026-01-01',
      employment_type: 'regular',
    })).rejects.toThrow();
  });

  test('directory listing', async () => {
    const state = loadState();
    const api = await createApiClient(BASE, state.adminToken);
    const res = await api.get('/api/v1/directory');
    expect(Array.isArray(res)).toBe(true);
  });
});
```

- [ ] **Step 3: Run tests**

Run: `cd e2e && npx playwright test --project=api tests/api/auth.spec.ts tests/api/employees.spec.ts`
Expected: All pass

- [ ] **Step 4: Commit**

```bash
git add e2e/tests/api/auth.spec.ts e2e/tests/api/employees.spec.ts
git commit -m "feat(e2e): API tests — auth and employees"
```

---

### Task 7: API Tests — Attendance, Leave, Payroll

**Files:**
- Create: `e2e/tests/api/attendance.spec.ts`
- Create: `e2e/tests/api/leave.spec.ts`
- Create: `e2e/tests/api/payroll.spec.ts`

**Test cases per module:**

`attendance.spec.ts`:
- GET /attendance/summary — returns data
- GET /attendance/records — with date filters
- POST /clock-in — employee can clock in
- POST /clock-out — employee can clock out
- GET /shifts — list shifts
- POST /shifts — admin can create shift
- Permission: employee cannot manage shifts

`leave.spec.ts`:
- GET /leave-types — list types
- GET /leaves — list requests
- GET /leaves — filter by status
- POST /leaves — employee submits request
- PUT /leaves/:id/approve — admin approves
- GET /leave-balances — check balances
- Permission: employee cannot approve

`payroll.spec.ts`:
- GET /payroll-cycles — list cycles
- POST /payroll-cycles — create cycle
- GET /payroll/payslips — list payslips
- GET /export/payroll/:id/csv — export CSV
- POST /employees/:id/salary — assign salary
- Permission: employee cannot manage payroll

Follow the same pattern as Task 6 (loadState, createApiClient, test assertions).

- [ ] **Step 1: Write all three test files following the pattern from Task 6**
- [ ] **Step 2: Run: `cd e2e && npx playwright test --project=api tests/api/attendance.spec.ts tests/api/leave.spec.ts tests/api/payroll.spec.ts`**
- [ ] **Step 3: Commit**

```bash
git add e2e/tests/api/attendance.spec.ts e2e/tests/api/leave.spec.ts e2e/tests/api/payroll.spec.ts
git commit -m "feat(e2e): API tests — attendance, leave, payroll"
```

---

### Task 8: API Tests — Benefits, Training, Performance, Compliance

**Files:**
- Create: `e2e/tests/api/benefits.spec.ts`
- Create: `e2e/tests/api/training.spec.ts`
- Create: `e2e/tests/api/performance.spec.ts`
- Create: `e2e/tests/api/compliance.spec.ts`

**Test cases:**

`benefits.spec.ts`: list plans, create plan, enroll employee, list enrollments, submit claim
`training.spec.ts`: list programs, create program, enroll employee, list enrollments, mark complete
`performance.spec.ts`: list reviews, create review, create goal, update goal, list goals
`compliance.spec.ts`: list forms, list alerts, generate form, list filings, create filing

- [ ] **Step 1: Write all four test files**
- [ ] **Step 2: Run and verify**
- [ ] **Step 3: Commit**

```bash
git add e2e/tests/api/benefits.spec.ts e2e/tests/api/training.spec.ts e2e/tests/api/performance.spec.ts e2e/tests/api/compliance.spec.ts
git commit -m "feat(e2e): API tests — benefits, training, performance, compliance"
```

---

### Task 9: API Tests — Dashboard, Analytics, Approval, Reports, ImportExport

**Files:**
- Create: `e2e/tests/api/dashboard.spec.ts`
- Create: `e2e/tests/api/analytics.spec.ts`
- Create: `e2e/tests/api/approval.spec.ts`
- Create: `e2e/tests/api/report.spec.ts`
- Create: `e2e/tests/api/importexport.spec.ts`

**Test cases:**

`dashboard.spec.ts`: GET /dashboard, GET /dashboard/widgets, GET /dashboard/stats
`analytics.spec.ts`: GET /analytics/flight-risk, /burnout, /team-health
`approval.spec.ts`: list workflows, create workflow, list pending approvals
`report.spec.ts`: list reports, generate report
`importexport.spec.ts`: export employees CSV, export attendance CSV, preview import CSV, import CSV

- [ ] **Step 1-3: Write, run, commit**

---

### Task 10: API Tests — Operations (Loan, Expense, Holiday, Knowledge, Policy, Docfile, Milestone, Overtime)

**Files:** 8 spec files in `e2e/tests/api/`

**Test cases per module:** CRUD operations + list + permission checks. Each 4-6 test cases.

- [ ] **Step 1-3: Write, run, commit**

---

### Task 11: API Tests — Communication & Employee Relations (Announcement, Recognition, Notification, Pulse, Disciplinary, Grievance, HRRequest)

**Files:** 7 spec files in `e2e/tests/api/`

- [ ] **Step 1-3: Write, run, commit**

---

### Task 12: API Tests — System & Integration (Company, Audit, Workflow, OrgIntel, SelfService, VirtualOffice, Integration, Onboarding, Clearance, FinalPay, Billing, Referral, AI, NPS, BYOK, BotLink)

**Files:** Remaining spec files in `e2e/tests/api/`

- [ ] **Step 1-3: Write, run, commit**

---

### Task 13: Browser Tests — Login, Setup Wizard, Dashboard

**Files:**
- Create: `e2e/tests/browser/login.spec.ts`
- Create: `e2e/tests/browser/setup-wizard.spec.ts`
- Create: `e2e/tests/browser/dashboard.spec.ts`

**Pattern:** Browser tests use the `auth.ts` fixtures for `adminContext`.

- [ ] **Step 1: Create login browser test**

```typescript
import { test, expect } from '@playwright/test';
import { loadState } from '../../fixtures/state';

const BASE = process.env.E2E_BASE_URL || 'https://halaos.com';

test.describe('Login Page', () => {
  test('login page loads', async ({ page }) => {
    await page.goto(BASE + '/login');
    await expect(page.locator('input[type="text"], input[name="email"]')).toBeVisible();
    await expect(page.locator('input[type="password"]')).toBeVisible();
  });

  test('login with valid credentials', async ({ page }) => {
    const state = loadState();
    await page.goto(BASE + '/login');
    await page.fill('input[type="text"], input[name="email"]', state.adminEmail);
    await page.fill('input[type="password"]', state.adminPassword);
    await page.click('button[type="submit"]');
    // Should redirect to setup wizard or dashboard
    await page.waitForURL(/\/(setup|$)/, { timeout: 15000 });
  });

  test('login with wrong password shows error', async ({ page }) => {
    await page.goto(BASE + '/login');
    await page.fill('input[type="text"], input[name="email"]', 'wrong@test.com');
    await page.fill('input[type="password"]', 'WrongPass123!');
    await page.click('button[type="submit"]');
    await expect(page.locator('.n-message, .n-alert, [role="alert"]')).toBeVisible({ timeout: 5000 });
  });
});
```

- [ ] **Step 2: Create dashboard browser test**

```typescript
import { test, expect } from '../../fixtures/auth';

test.describe('Dashboard Page', () => {
  test('dashboard loads with widgets', async ({ adminContext }) => {
    const page = await adminContext.newPage();
    await page.goto('/');
    await page.waitForLoadState('networkidle');
    // Dashboard should show content, not setup wizard
    await expect(page.locator('text=Dashboard, .dashboard, [class*=dashboard]').first()).toBeVisible({ timeout: 10000 });
  });

  test('dashboard shows employee count', async ({ adminContext }) => {
    const page = await adminContext.newPage();
    await page.goto('/');
    await page.waitForLoadState('networkidle');
    // Should show some numeric data
    const text = await page.textContent('body');
    expect(text).toBeTruthy();
  });
});
```

- [ ] **Step 3: Run and commit**

---

### Task 14: Browser Tests — Employee Management (Employees, Detail, Departments, Positions, Users, Directory)

**Files:** 6 spec files in `e2e/tests/browser/`

**Test cases:**
- employees.spec.ts: page loads, list visible, search works, add employee modal, export CSV
- employee-detail.spec.ts: detail page loads, tabs switch, edit saves
- departments.spec.ts: list loads, create department, edit
- positions.spec.ts: list loads, create position
- users.spec.ts: user list loads
- directory.spec.ts: directory loads, search works, org chart renders

- [ ] **Step 1-3: Write, run, commit**

---

### Task 15: Browser Tests — Attendance & Scheduling

**Files:** `attendance.spec.ts`, `attendance-records.spec.ts`, `dtr.spec.ts`, `schedules.spec.ts`, `geofence.spec.ts`

- [ ] **Step 1-3: Write, run, commit**

---

### Task 16: Browser Tests — Leave & Payroll

**Files:** `leave.spec.ts`, `leave-calendar.spec.ts`, `payroll.spec.ts`, `payslips.spec.ts`, `salary-config.spec.ts`

- [ ] **Step 1-3: Write, run, commit**

---

### Task 17: Browser Tests — HR Modules (Benefits, Training, Performance, Onboarding, Overtime, Milestones)

**Files:** 6 spec files

- [ ] **Step 1-3: Write, run, commit**

---

### Task 18: Browser Tests — Operations & Compliance

**Files:** `loans.spec.ts`, `expenses.spec.ts`, `import-export.spec.ts`, `compliance.spec.ts`, `tax-filings.spec.ts`, `approval.spec.ts`, `reports.spec.ts`

- [ ] **Step 1-3: Write, run, commit**

---

### Task 19: Browser Tests — Communication, System, Advanced

**Files:** All remaining browser spec files (~20 files): announcements, recognition, knowledge, policies, holidays, notifications, settings, profile, audit, virtual-office, 201-file, final-pay, clearance, billing, referrals, agent-hub, integrations, workflow-rules, pulse-surveys, hr-requests, org-intelligence, self-service, analytics

- [ ] **Step 1-3: Write, run, commit**

---

### Task 20: H5 Mobile Browser Tests

**Files:**
- Create: `e2e/tests/h5/home.spec.ts`
- Create: `e2e/tests/h5/attendance.spec.ts`
- Create: `e2e/tests/h5/leave.spec.ts`
- Create: `e2e/tests/h5/payslips.spec.ts`
- Create: `e2e/tests/h5/notifications.spec.ts`
- Create: `e2e/tests/h5/profile.spec.ts`
- Create: `e2e/tests/h5/ai-chat.spec.ts`

**Pattern:** Same as browser tests but with mobile viewport (375x812). Use employee context.

**Test cases per page:**
- home: page loads, quick actions visible, navigation works
- attendance: clock in/out buttons, status display
- leave: apply form, balances visible
- payslips: payslip list, download
- notifications: list, mark read
- profile: info visible, edit works
- ai-chat: chat input, send message

- [ ] **Step 1-3: Write, run, commit**

---

### Task 21: CI/CD & Cleanup

**Files:**
- Create: `.github/workflows/e2e.yml`
- Create: `e2e/fixtures/cleanup.ts`

- [ ] **Step 1: Create cleanup script**

```typescript
import { createApiClient } from './api-client';

async function cleanup() {
  const baseURL = process.env.E2E_BASE_URL || 'https://halaos.com';
  console.log('Cleanup: Would delete E2E-Test-* companies older than 7 days');
  console.log('Note: Requires admin API for company deletion or direct DB access');
  // Placeholder — implement when company deletion API is available
}

cleanup().catch(console.error);
```

- [ ] **Step 2: Create GitHub Actions workflow**

```yaml
name: E2E Tests

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * 0'  # Sunday 3am UTC

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: cd e2e && npm ci
      - name: Install Playwright
        run: cd e2e && npx playwright install chromium --with-deps
      - name: Run E2E tests
        run: cd e2e && npx playwright test
        env:
          E2E_BASE_URL: https://halaos.com
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-report
          path: |
            e2e/playwright-report/
            e2e/test-results/
          retention-days: 14
```

- [ ] **Step 3: Run full suite locally**

Run: `cd e2e && npm run test:all`
Expected: Factory seeds data, API tests run, browser tests run, report generated

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/e2e.yml e2e/fixtures/cleanup.ts
git commit -m "feat(e2e): CI/CD workflow and cleanup script"
```

---

## Verification Checklist

After all tasks complete:

1. `cd e2e && npm run test:all` — Full suite passes
2. `npx playwright show-report` — HTML report opens with results
3. `.test-state.json` exists with populated IDs and tokens
4. No hardcoded credentials (all from factory-generated data)
5. GitHub Actions workflow valid: `gh workflow view e2e.yml`
